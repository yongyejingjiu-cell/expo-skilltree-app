# エラー解説：Unable to resolve module ...

## どんなエラー？
「`@react-navigation/native` という道具を使おうとしたけど、道具箱（`node_modules`）の中に見つからないよ！」というエラーです。

## なぜ起きたの？
料理で例えると、「レシピ（コード）には『オリーブオイルを使う』と書いてあるのに、キッチン（プロジェクト）にオリーブオイルが買ってない」状態です。
先ほどインストールコマンドを実行しましたが、ネットワークの不調やタイミングの問題で、実際にはキッチンに届いていなかった（インストールが完了していなかった）ようです。

## 対処法
「買い物に行ってくる（インストールをやり直す）」ことで解決します。
具体的には、もう一度コマンドを実行して、確実に道具を揃えます。
